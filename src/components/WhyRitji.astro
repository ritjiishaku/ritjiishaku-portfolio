---
const credentials = [
  "5+ years active clinical nursing experience — I have worked the systems I now automate",
  "Certified AI Automation Engineer — trained in LLMs, AI agents, RAG systems, and multi-agent workflows",
   "UX/UI Designer — I design for the nurse at the front desk, not the engineer who built the system.",
  "I build systems that clinical staff will actually use — because I have been that staff"
];
---

<section id="why-me" class="bg-[#F7F9FC] py-[48px] md:py-[80px] px-5 md:px-0">
  <div class="max-w-container mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16 items-start">
      <div class="reveal">
        <div class="text-[11px] font-medium uppercase tracking-[0.14em] text-blue mb-4 font-body">Why Me</div>
        <h2 class="text-[26px] md:text-[38px] font-bold text-navy mb-6 font-display leading-[1.2]">
          Any developer can build an automation.<br /> 
          Very few understand what happens inside<br /> 
          a clinic when it breaks.
        </h2>
        <p class="text-[16px] text-muted max-w-[580px] mb-8 font-body leading-[1.65]">
          I spent 5 years as a registered nurse watching health businesses 
          fail — not from poor care, but poor systems. Now I build 
          the systems that fix exactly that. Every automation I design, I have personally needed on a ward at 2am. That changes what I build and how I build it.
        </p>

        <div class="flex flex-col">
          {credentials.map((item, index) => (
            <div class={`flex gap-3 py-3 ${index !== credentials.length - 1 ? 'border-b border-[#E2E8F0]' : ''}`}>
              <div class="w-[6px] h-[6px] rounded-full bg-blue mt-[7px] shrink-0"></div>
              <p class="text-[13px] text-navy leading-[1.8] font-body">{item}</p>
            </div>
          ))}
        </div>
      </div>

      <div class="reveal reveal-delay-2 mt-8 md:mt-[100px]">
        <blockquote class="border-l-4 border-blue rounded-r-[8px] bg-blue/[0.06] p-5 md:p-6">
          <p class="text-[16px] italic text-navy leading-[1.65] font-display">
            "I'm not just a tech person. I'm a nurse who 
            built these systems because I lived the problem."
          </p>
          <footer class="mt-2 text-[10px] uppercase font-medium text-blue tracking-[0.1em] font-body">
            — Ritji Ishaku
          </footer>
        </blockquote>
      </div>
    </div>
  </div>
</section>
